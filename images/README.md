# Your photos go here

Place your photo files in this folder. The default gallery expects names like `photo1.jpg`, `photo2.jpg`, and so on. You can use different names by changing the paths in `js/script.js`.
